name: Website Monitor

on:
  schedule:
    # Runs every hour at minute 0. Change '0' to '*/30' for every 30 mins.
    - cron: '0 * * * *'
  
  # Allows manual testing
  workflow_dispatch:

jobs:
  check-website:
    runs-on: ubuntu-latest

    steps:
    # 1. Download your code from the repo
    - name: Checkout Repository
      uses: actions/checkout@v3

    # 2. Install Python
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    # 3. Install libraries needed for the script
    - name: Install Dependencies
      run: |
        pip install requests beautifulsoup4

    # 4. Run your script
    - name: Run Monitor Script
      env:
        # This passes your Secrets to the script safely
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
      run: |
        python monitor.py

    # 5. Save the new "baseline" hash back to the repo
    # This ensures the next run knows what the website looked like this time
    - name: Commit Updated Baseline
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add baseline_hash.txt
        # Only commit if there are changes
        git diff --staged --quiet || git commit -m "Update baseline hash [skip ci]"
        git push
